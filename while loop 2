#include <stdio.h>

int main() {
    int num;

    printf("Enter numbers (0 to stop):\n");

    while (1) {   
        scanf("%d", &num);

        if (num == 0) {
            break;  
        }

        printf("You entered: %d\n", num);
    }

    return 0;
}
